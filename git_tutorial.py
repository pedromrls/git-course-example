def add_numbers(a,b,c):
    return sum(a,b,c)

# 1. extend add_numbers to allow an arbitrary number of parameters
# 2. investigate and resolve security explout of add_numbers()
# 3. make the implementation of add_numbers() more performant
# 4. add documentation for add_number()

def cal_diff(a, b):
    return a - b 